spring:
  application:
    name: CONFIG-SERVER-DEV

eureka:
  instance:
    #instance-id: ${spring.application.name}:${server.port}
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-withe-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
